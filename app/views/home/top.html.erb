<div class="main-visual">
  <div class="main-title">
    <p class="title">バドラー！<p>
    <p class="text">バドラーのバドラーによるバドラーのためのWebサイト</p>
  </div>
</div>
<div class="select-boxes">
  <div class="box1">
    <h3>新着クラブ</h3>
    <div class="content-boxes">
      <% @clubs.each do |club| %>
        <%= link_to club_path(club.id) do %>
          <div class="content-box">
            <h5><span>◆</span> <%= club.club_name%></h5>
            <h6>エリア：<%= club.practice_area %></h6>
            <h6>競技レベル：<%= club.club_level %></h6>
          </div>
        <% end %>
      <% end %>
    </div>
    <%= link_to "クラブ一覧へ", clubs_path, class: "club-link" %>
  </div>
  <div class="box2">
    <h3>バドラー！とは</h3>
    <div class="content-box2">
      <p>バドラー！はバドミントンを愛する全ての人のためのポータルサイトです。<br>会員登録を行うと大会へのエントリー、気になるクラブへの参加希望のやりとりなどがサイト上で可能になります。<br>また、大会の結果により得られるポイントの合計値で全国のバドラーと「バドラーランク」を競い合います！<br>画面の前のあなたも今すぐ会員登録して「バドランカー」の一員になりませんか？</p>
    </div>
    <% if user_signed_in? %>
      <div class="space">
        <%= link_to "マイページへ",user_path(current_user.id), class: "mypage" %>
      </div>
    <% else %>
      <ul>
        <li><%= link_to "会員登録を行う", new_user_registration_path, class: "signup" %></li>
        <li><%= link_to "ログインして始める", new_user_session_path, class: "login" %></li>
      </ul>
    <% end %>
  </div>
  <div class="box3">
    <h3>新着大会</h3>
    <div class="content-boxes3">
      <% @events.each do |event| %>
        <%= link_to event_path(event.id) do %>
          <div class="content-box3">
            <h5 class="event"><span>◆</span> <%= event.event %></h5>
            <h6 class="event">開催日：<%= event.date.strftime('%Y/%m/%d') %></h6>
            <h6>会場：<%= event.event_place %></h6>
            <h6>エントリー<%= event.deadline.strftime('%Y/%m/%d') %>まで</h6>
          </div>
        <% end %>
      <% end %>
    </div>
    <%= link_to "大会一覧へ", events_path,  class: "event-link" %>
  </div>
</div>